/**
 * Phase d is responsible for transforming all methods with an atomic annotations so that they
 * are transactional.
 *
 * This is done by transforming methods of classes with the @annotation, so that the use a
 * TransactionTemplate like structure.
 *
 * @author Peter Veentjer.
 */
TransformClass {

}