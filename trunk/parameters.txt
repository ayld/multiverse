#a flag that is used to enable sanity checks.
sanitychecks.enabled default=true

# a flag that enables the reuse of the FailedToObtainLocksException exception instance
reuse.org.multiverse.api.exceptions.FailedToObtainLocksException default = true

# a flag that enables the reuse of the LoadLockedException exception instance
reuse.org.multiverse.api.exceptions.LoadLockedException default = true

# a flag that enables the reuse of the RetryError exception instance
reuse.org.multiverse.api.exceptions.RetryError default = true

# a flag that enables the reuse of the LoadTooOldVersionException exception instance
reuse.org.multiverse.api.exceptions.LoadTooOldVersionException default = true

# a flag that enables the reuse of the WriteConflictException exception instance
reuse.org.multiverse.api.exceptions.WriteConflictException default = true

# a flag that enables automatic printing of the Profiler information (if available)
# of the stm stores in GlobalStmInstance.
multiverse.profiler.printthread.enabled default = false
# the delay in miliseconds between prints
multiverse.profiler.printthread.delayms default = 2000
# the location to write to
multiverse.profiler.printthread.location =